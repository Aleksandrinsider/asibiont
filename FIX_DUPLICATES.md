# Исправления 06.01.2026 21:00

## Проблемы
1. ❌ Дублирующиеся задачи в панели (каждая задача по 2 раза)
2. ❌ Двойная @@ в делегированных задачах ("для @@testuser")
3. ❌ Отсутствие текста в скобках для делегированных задач

## Исправления

### 1. Удалены дубликаты задач
```python
# remove_duplicates.py
Удалены задачи: 130, 131, 132, 133
Осталось задач: 4 (126, 127, 128, 129)
```

### 2. Обновлено форматирование в main.py
**До:**
```python
title = f"{task.title} для @{task.delegated_to_username}"  # для @@testuser
```

**После:**
```python
delegated_username = task.delegated_to_username.lstrip('@')
title = f"{task.title} (делегирована для @{delegated_username})"  # (делегирована для @testuser)
```

### 3. Обновлены фильтры в dashboard_new.html
**До:**
```javascript
t.title.includes('для @')
t.title.includes('от @')
```

**После:**
```javascript
t.title.includes('(делегирована для @')
t.title.includes('(делегирована от @')
```

## Результат

### Обычные задачи:
- "Позвонить клиенту Иванову"
- "Подготовить презентацию"

### Делегированные мной задачи:
- "Проверить документы (делегирована для @testuser)" ✅

### Делегированные мне задачи:
- "Задача (делегирована от @username)" ✅

## Коммит
```
5692b9e - Fix: remove duplicate tasks, fix delegation display format
```

Изменения отправлены на Railway. После деплоя панель покажет 4 задачи (вместо 8 дублей) с правильным форматированием.
