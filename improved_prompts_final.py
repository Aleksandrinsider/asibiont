# Улучшенная система промптов - исправленная версия
import re
from datetime import timedelta

CORE_SYSTEM_PROMPT = """Ты - ASI Biont, AI-помощник по задачам. Контекстуально осведомлённый диалог, конкретные действия, проактивные рекомендации.

База правил 0: Шутливый тон (предпочитай действием!)
Тогда анализируй контекст перед ответом:
1. Что пользователь реально хочет? (Сквозной анализ контекста)
2. Какие факторы влияют? (Время, ресурсы, приоритеты, люди)
3. Что будет дальше? (Прогноз последствий)
4. Кого не хватает? (Проверь информативность)

База правил 1: Шаблонные ответы
Используй функции вместо текста для задач:
- Ищи @username? → delegate_task()
- "Покажи задачи" → list_tasks()
- "Напомни X" → add_task()
- "Сделал X" → complete_task()
- "Удали все" → delete_all_tasks()

Уровни ответа (после вызовов):
Минимум 3-4 предложения:
1. Что сделал (факты)
2. Почему полезно (контекст)
3. Что дальше (insight/предложение)
4. Дополнительная информация или доп. контекст (опционально)

Примеры:
Простой: "Добавил задачу "добавь белок""
Сложный: "Добавил список на завтра в 15:00. Это даст время на подготовку доклада. Кстати, @ivan тоже готовит презентацию - можешь синхронизироваться?"

Правила ответа: жирный текст, списки, markdown

Управление ответами AI системы:
- Минимум 3-4 предложения для каждого
- Для simple действий: 3-4 предложения с контекстом
- Для анализа/совета: 5-7 предложений
- Не отвечай одним предложением!
- Примеры ответов: "Готово", "Добавил задачу", "Вот список"
- Примеры сложных: "Добавил задачу на завтра. Это поможет не забыть важное. Кстати, можешь добавить что-то ещё в список - проверь почту?"

Ты - AI помощник - Максимально полезный:
Критичные правила (!):
- Не "Добавил X. Что дальше?" каждый раз
- Добавь анализ: "Добавил/Показал/Понял/Узнал/Интересно/Шутка"
- Флин: короткие (2-3 предложения) для простых, длинные (4-5) для сложных
- Не повторяй одним предложением!
- Примеры ответов: "Готово", "Добавил задачу", "Вот список"
- Примеры сложных: "Добавил задачу на завтра. Это поможет не забыть важное. Кстати, можешь добавить что-то ещё в список - проверь почту?"""


def improved_classify_intent(message: str, mentions_str: str = "") -> dict:
    """Улучшенная классификация намерений пользователя с поддержкой русского языка"""
    message_lower = message.lower().strip()

    # Шаблоны для различных команд
    patterns = {
        'add_task': [
            r'напомни(?:ть)?\s+(.+)',
            r'добавь\s+(.+)',
            r'запомни\s+(.+)',
            r'создай\s+задачу\s+(.+)',
            r'новая\s+задача\s+(.+)',
            r'задача\s+(.+)',
            r'напомни\s+о\s+(.+)',
            r'(.+)\s+напомни',
            r'(.+)\s+добавь',
            r'(.+)\s+запомни'
        ],
        'list_tasks': [
            r'покажи\s+задачи',
            r'список\s+задач',
            r'мои\s+задачи',
            r'что\s+на\s+сегодня',
            r'что\s+запланировано',
            r'какие\s+задачи',
            r'список',
            r'задачи'
        ],
        'complete_task': [
            r'сделал\s+(.+)',
            r'выполнил\s+(.+)',
            r'завершил\s+(.+)',
            r'готово\s+(.+)',
            r'готово',
            r'сделано\s+(.+)',
            r'завершить\s+(.+)'
        ],
        'delegate_task': [
            r'@(\w+)\s+(.+)',
            r'поручи\s+@(\w+)\s+(.+)',
            r'делегируй\s+@(\w+)\s+(.+)',
            r'передай\s+@(\w+)\s+(.+)'
        ],
        'delete_all_tasks': [
            r'удали\s+все',
            r'очисти\s+все',
            r'удалить\s+все\s+задачи',
            r'очистить\s+список'
        ]
    }

    # Проверяем каждый паттерн
    for intent_type, pattern_list in patterns.items():
        for pattern in pattern_list:
            match = re.search(pattern, message_lower)
            if match:
                return {
                    "type": intent_type,
                    "confidence": 0.9,
                    "params": {}
                }

    # Если ничего не найдено, возвращаем 'chat'
    return {
        "type": "chat",
        "confidence": 0.5,
        "params": {}
    }


def get_optimized_prompt_final(user_now=None, current_time_str=None, user_username=None, mentions_str=None, user_memory=None) -> str:
    """Возвращает оптимизированный системный промпт с динамическими данными"""
    base_prompt = CORE_SYSTEM_PROMPT
    
    # Добавляем контекст пользователя если доступен
    context_parts = []
    if user_now:
        context_parts.append(f"Текущее время: {user_now}")
    if current_time_str:
        context_parts.append(f"Время для пользователя: {current_time_str}")
    if user_username:
        context_parts.append(f"Имя пользователя: {user_username}")
    if mentions_str:
        context_parts.append(f"Упоминания: {mentions_str}")
    if user_memory:
        context_parts.append(f"Память пользователя: {user_memory}")
    
    if context_parts:
        context_str = "\n".join(context_parts)
        base_prompt = f"{base_prompt}\n\nКонтекст пользователя:\n{context_str}"
    
    return base_prompt


def improved_fallback(intent: dict, tool_calls=None, ai_response_content="", message="", user_id=None) -> str:
    """Резервная функция для обработки сообщений"""
    return "chat"
